# Microverse 配置系统说明

## 配置的三层结构

### 1️⃣ 代码默认值（最底层）
**文件位置**: `script/ui/SettingsManager.gd` 第 14-23 行

```gdscript
var current_settings = {
    "api_type": "Ollama",
    "model": "llama3.2:3b",  // ✅ 我们刚刚改的
    "api_key": "",
    "show_ai_model_label": true,
    "window_mode": "windowed",
    "screen_width": 1280,
    "screen_height": 720
}
```

**作用**: 
- 这是**第一次运行游戏**或**没有保存配置文件**时使用的默认值
- 仅在 `user://settings.cfg` 文件不存在时生效

---

### 2️⃣ 保存的配置文件（中间层）
**文件位置**: `~/Library/Application Support/Godot/app_userdata/Microverse/settings.cfg`

**工作流程**:
```
游戏启动 → SettingsManager._ready()
         ↓
   load_settings() 检查配置文件是否存在
         ↓
   ┌─ 存在 → 读取文件内容，覆盖 current_settings
   └─ 不存在 → 使用代码中的默认值
```

**重要**: 一旦这个文件存在，代码中的默认值就会被**忽略**！

---

### 3️⃣ 游戏 UI 显示（最上层）

游戏中有多个地方显示和修改配置：

#### A. 全局设置 UI
**文件**: `script/ui/GlobalSettingsUI.gd`
- 按 ESC → 打开设置面板
- 显示和修改全局默认 AI 配置
- 保存后更新 `settings.cfg`

#### B. 角色头顶标签
**文件**: `script/ui/AIModelLabel.gd`
- 显示每个角色当前使用的模型名
- 从 SettingsManager 读取配置
- **实时显示**: 这个就是你看到的 "qwen1.5b"

#### C. 角色独立设置
**文件**: `script/ui/CharacterAISettings.gd`
- 可以为每个角色单独配置不同的模型
- 保存到独立文件: `character_ai_settings.cfg`

---

## 你遇到的问题解释

### 为什么游戏显示 `qwen1.5b`？

```
游戏启动
  ↓
SettingsManager._ready()
  ↓
load_settings()
  ↓
发现配置文件存在: ~/Library/.../settings.cfg
  ↓
读取文件内容: {"model": "qwen1.5b", ...}  ← 旧的配置
  ↓
覆盖 current_settings
  ↓
AIModelLabel 显示: "qwen1.5b"  ← 你看到的
```

虽然我们修改了代码中的默认值为 `llama3.2:3b`，但配置文件中保存的还是旧值 `qwen1.5b`。

---

## 配置读取的优先级

```
优先级 (从高到低):

1. 角色独立配置 (character_ai_settings.cfg)
   ↓
2. 用户保存的配置 (settings.cfg) ← 你的问题在这
   ↓
3. 代码默认值 (SettingsManager.gd)
```

---

## 三种解决方案

### 方案 1: 删除配置文件（推荐）✨

```bash
# 删除全局配置
rm ~/Library/Application\ Support/Godot/app_userdata/Microverse/settings.cfg

# 删除角色配置（如果存在）
rm ~/Library/Application\ Support/Godot/app_userdata/Microverse/character_ai_settings.cfg

# 重启游戏 → 使用新的默认值 llama3.2:3b
```

### 方案 2: 在游戏 UI 中手动切换

```
1. 启动游戏
2. 按 ESC 打开设置
3. 找到 "模型" 下拉菜单
4. 选择 "llama3.2:3b"
5. 点击"保存"
```

### 方案 3: 手动编辑配置文件

```bash
# 1. 找到配置文件
open ~/Library/Application\ Support/Godot/app_userdata/Microverse/

# 2. 编辑 settings.cfg，修改模型名
# 将 "qwen1.5b" 改为 "llama3.2:3b"
```

---

## 配置文件内容示例

**settings.cfg 内容**:
```json
{
  "api_type": "Ollama",
  "model": "llama3.2:3b",
  "api_key": "",
  "show_ai_model_label": true,
  "window_mode": "windowed",
  "screen_width": 1280,
  "screen_height": 720
}
```

---

## 代码流程详解

### 1. 游戏启动时

```gdscript
# SettingsManager.gd
func _ready():
    _load_api_config()
    load_settings()  # ← 关键：这里读取配置

func load_settings():
    if FileAccess.file_exists(CONFIG_FILE):  # ← 检查文件
        # 读取并解析 JSON
        current_settings = data  # ← 覆盖默认值
    else:
        # 使用代码中的默认值
```

### 2. UI 显示时

```gdscript
# AIModelLabel.gd
func load_settings():
    var settings_manager = get_node("/root/SettingsManager")
    current_settings = settings_manager.get_character_ai_settings(character_name)
    # ↑ 从 SettingsManager 获取配置

func update_display_text():
    text = current_settings.model  # ← 显示到标签
```

### 3. 保存设置时

```gdscript
# SettingsManager.gd
func update_settings(new_settings: Dictionary):
    current_settings = new_settings.duplicate()
    save_settings()  # ← 保存到文件
    settings_changed.emit(current_settings)  # ← 通知所有监听者

func save_settings():
    var file = FileAccess.open(CONFIG_FILE, FileAccess.WRITE)
    file.store_string(JSON.stringify(current_settings))
    # ↑ 写入 settings.cfg
```

---

## 调试技巧

### 查看当前使用的配置

在游戏控制台中添加：

```gdscript
# 在任意脚本中
print("当前配置:", SettingsManager.get_settings())
```

### 查看配置文件

```bash
# macOS
cat ~/Library/Application\ Support/Godot/app_userdata/Microverse/settings.cfg

# 查看是否存在
ls -la ~/Library/Application\ Support/Godot/app_userdata/Microverse/
```

---

## 最佳实践

### 推荐的配置更新流程

1. **修改代码默认值** (SettingsManager.gd) ✅ 已完成
2. **删除旧的配置文件** (让游戏使用新默认值)
3. **启动游戏验证** (检查显示是否正确)
4. **在 UI 中保存** (生成新的配置文件)

### 开发时的注意事项

- 修改代码默认值后，测试时要删除配置文件
- 配置文件的优先级高于代码
- 每个角色可以有独立配置
- 设置更改通过信号系统实时同步

---

## 总结

**你的问题**：
- 代码已改为 `llama3.2:3b` ✅
- 但游戏显示 `qwen1.5b` ❌

**原因**：
- 配置文件 `settings.cfg` 中保存的是旧值
- 文件配置优先级高于代码默认值

**解决**：
```bash
# 删除旧配置，让游戏使用新默认值
rm ~/Library/Application\ Support/Godot/app_userdata/Microverse/settings.cfg

# 重启游戏即可 ✅
```

---

## 架构图

```
┌─────────────────────────────────────────┐
│          游戏启动                          │
└──────────────┬──────────────────────────┘
               ↓
┌──────────────────────────────────────────┐
│    SettingsManager._ready()               │
│    ↓                                      │
│    load_settings()                        │
│    ↓                                      │
│    配置文件存在？                          │
│    ├─ Yes → 读取 settings.cfg (旧值)     │
│    └─ No  → 使用代码默认值 (新值)         │
└──────────────┬──────────────────────────┘
               ↓
┌──────────────────────────────────────────┐
│    current_settings 已设置                │
└──────────────┬──────────────────────────┘
               ↓
┌──────────────────────────────────────────┐
│    UI 组件读取配置                         │
│    ├─ AIModelLabel (显示模型名)           │
│    ├─ GlobalSettingsUI (设置面板)         │
│    └─ CharacterAISettings (角色设置)      │
└──────────────────────────────────────────┘
```


